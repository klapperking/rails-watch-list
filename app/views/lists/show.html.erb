<h1><%= @list.name %></h1>

<ul>
  <% @list.movies.each do |movie| %>
    <li>
      <%= movie.title %>
      <%= movie.rating %>
    </li>
  <% end %>
</ul>

<%= link_to('Destroy this list', list_path(@list), data: { turbo_method: :delete }) %>
